package net.minecraft.client.stream;

public enum ChatController$ChatState {
   Uninitialized,
   Initialized,
   Connecting,
   Connected,
   Disconnected;

   private ChatController$ChatState() {
   }
}
